import { AssignType } from "../client";
export class WeverseMedia extends AssignType() {
    constructor(props, community) {
        super(props);
        this.community = community;
    }
    toJSON() {
        const partial = Object.assign({}, this);
        delete partial.community;
        return partial;
    }
}
